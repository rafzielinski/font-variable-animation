{
  "scripts": {
    "start": "parcel serve index.html --log-level verbose --no-cache"
  },
  "dependencies": {
    "parcel": "^2.8.3",
    "splitting": "^1.0.6"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.8.3"
  }
}
